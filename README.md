# 180-cubemap-to-panorama
simple unity program that converts any kinda unity skybox you can even convert a panorama to a panorama (im not sure what the point of that is) but it works. TO USE: run this in the unity editor ONLY compileing it to an actual app will leave you with a dummy app that just shows part of the skybox. the file is at Assets/panorama.png
